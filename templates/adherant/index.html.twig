<html>
<head>

</head>
<body>
<center>
    <table border="2">
        <tr><th>id</th><th>nom</th><th>classe</th></tr>
        <tr{% for p in list %}>
            <td>{{ p.id }}</td>
            <td>{{ p.nom }}</td>
            <td>{{ p.Sexe }}</td>
            <td>{{ p.DateDeNaissance }}</td>
            <td>{{ p.Adresse }}</td>
            <td>{{ p.NumeroDeTel }}</td>
            <td>{{ p.NumeroDeTel }}</td>
        </tr {% endfor %}>

    </table>
</center>
</body>
</html>